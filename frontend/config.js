// This URL is public configuration, not a secret.
// Replace it with the production URL Vercel assigns to the backend.
window.APP_CONFIG = {
  apiBaseUrl: "https://YOUR_VERCEL_PROJECT.vercel.app",
};

